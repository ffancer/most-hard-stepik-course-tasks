Напишите функцию modify_list(l), которая принимает на вход список целых чисел, 
удаляет из него все нечётные значения, а чётные нацело делит на два.
Функция не должна ничего возвращать, требуется только изменение переданного списка, например:


lst = [1, 2, 3, 4, 5, 6]

def modify_list(l):
    i = len(l) - 1  # создаем переменную что бы небыло бесконечного цикла(которая меньше длинны списка на единицу, можно обойтись только i
    i = le        # создаем переменную равную длинне списка минус единица, что бы уменьшать её в дальнейшем
    while i != -1:  # пока i не будет равна последнему индексу списка
        if l[i] % 2:  # если элемент четный то
            del l[i]  # удаляем
        else:  # иначе
            l[i] = l[i] // 2  # наш элмент становтся элементом, нацело деленным на 2
        i -= 1  # уменьшаем на единицу, двигаемcя в обратно порядке
    return
    
print(modify_list(lst))  # None
print(lst)  # [1, 2, 3]
modify_list(lst)
print(lst)               # [1]

lst = [10, 5, 8, 3]
modify_list(lst)
print(lst)
