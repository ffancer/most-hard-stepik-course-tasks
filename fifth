Напишите программу, которая принимает на вход список чисел в одной строке и выводит на экран в одну строку значения, которые встречаются в нём более одного раза.

Для решения задачи может пригодиться метод sort списка.

Выводимые числа не должны повторяться, порядок их вывода может быть произвольным.


num_lst = [int(i) for i in input().split()]   # вводим 4 8 0 3 4 2 0 3
num_lst.sort()  # при копировании получаем упорядоченный список [0, 0, 2, 3, 3, 4, 4, 8]
out = []  # пустой список для вывода ответа 
for i in range(len(num_lst)):  # для i в диапазоне длинны нашего упорядоченного списка, в данном случае это 8 цифр
    if i > 0:                  # если i больше нуля
        if num_lst[i] == num_lst[i - 1]:  # тут сравнение, ноль из списка не учитывается т.к. в условии i>0, т.е первое сравнение 2 и 0, потом 3 и 2, 3 и 3 - равны, идем дальше
            if num_lst[i] not in out:     # если нашей 3 нет в списке out, то...
              out += [num_lst[i]]     # добавляем тройку в out, можно заменить запись на out.append(num_lst[i])
for i in out:  # выводим ответ из нашего списка out в принт в красивом виде, символы между собой разделены пробелом, этому способствует end=' '
    print(i, end=" ")
